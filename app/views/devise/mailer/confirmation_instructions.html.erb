<div class="email-container">
  <p><%= t('.welcome', email: @email) %></p>

  <p><%= t('.confirm_text') %></p>

  <p><%= link_to t('.confirm_link'),
         confirmation_url(@resource, confirmation_token: @token),
         class: "button-link" %></p>
</div>
