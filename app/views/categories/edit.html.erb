<%= turbo_frame_tag @category do %>
  <div class='hover:cursor-grab flex justify-between items-center px-6 py-4 hover:bg-gray-50 dark:hover:bg-gray-800 transition-colors border-l-4 border-sky-500' data-id="<%= @category.id %>">
    <%= render 'inline_form', category: @category %>
  </div>
<% end %>