<div class="flex flex-col w-[90%] mb-20">
  <%= render 'form',
      wine: @wine,
      denominations: @denominations,
      message: t('wines.form.title.edit') %>
  <%= link_to t('wines.common.back'),
      control_panel_products_path(filter: 'wines'),
      class: 'underline underline-blue-800 text-blue-600' %>
</div>
