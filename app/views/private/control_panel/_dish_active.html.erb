<%= turbo_frame_tag "dish_active_#{dish.id}" do %>
  <%= link_to '🔴', private_toggle_active_path(id: dish.id, type: 'dish'), class: "no-underline select-none text-xl", data: { turbo_method: :post } if !dish.active%>
  <%= link_to '🟢', private_toggle_active_path(id: dish.id, type: 'dish'), class: "no-underline select-none text-xl", data: { turbo_method: :post } if dish.active%>
<% end %>
