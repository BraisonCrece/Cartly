<div class='flex justify-between gap-2 px-4 py-6 border-t border-gray-100 text-brand-gray dark:text-gray-200 print:my-6 dark:border-gray-100/25 animate__animated animate__fadeIn animate__slower active:scale-[0.98] active:opacity-80 transition-all cursor-pointer'
     role="article"
>
  <div class="title price flex flex-col max-w-[72.5%]">
    <h2 class='text-2xl font-light playfare balance'>
      <%= drink.name %>
    </h2>
    <% if drink.measure.present? && drink.unit.present? %>
      <p class='text-sm font-light text-gray-500 dark:text-gray-400 mt-1'>
        <%= drink.measure %><%= drink.unit %>
      </p>
    <% end %>
  </div>
  <div class="flex self-center gap-1 text-2xl font-extralight">
    <%= number_with_precision(drink.price, precision: 2, separator: ",") %> €
  </div>
</div>
