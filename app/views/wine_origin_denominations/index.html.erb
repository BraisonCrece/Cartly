<div class='w-full max-w-3xl mx-auto p-3'>
  <h1 class="font-thin text-4xl text-center mb-10 text-pretty"><%= t('.title') %></h1>
  <%= render "form" %>
  <%= turbo_frame_tag @denominations_list_frame_tag, class: "flex flex-col gap-3" do %>
    <%= render @denominations %>
  <% end %>
</div>
