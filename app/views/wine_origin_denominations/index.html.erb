<div class='w-full px-6 mx-auto p-3 mb-20'>
  <h1 class="text-center font-bold text-3xl mb-10 text-pretty">Denominaciones de origen</h1>
  <%= render "form" %>
  <%= turbo_frame_tag @denominations_list_frame_tag, class: "flex flex-col gap-3" do %>
    <%= render @denominations %>
  <% end %>
</div>
