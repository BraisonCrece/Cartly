<% if products?(params[:restaurant_id]) %>
  <%= render "shared/info" %>
<% else %>
  <% if params[:restaurant_id] %>
    <div class="border rounded-md p-4 mt-24 flex max-w-md w-[80%] mx-auto">
      <div class="flex items-center justify-center gap-4">
        <%= image_tag "info.svg", class: "w-6", alt: t('.info_icon'), aria_hidden: "true" %>
        <p class="text-lg font-light italic prose-rose text-balance text-center">
          La carta está siendo diseñada, por favor, vuelva pronto!🙂
        </p>
      </div>
    </div>
  <% elsif request.path.include?('admin/productos') && !products?(current_restaurant.id, active: false) %>
    <div class="fixed bottom-0 left-0 right-0 mb-6 px-4">
      <div class="bg-white dark:bg-gray-900 rounded-lg shadow-lg border border-gray-200 dark:border-gray-700 overflow-hidden max-w-md mx-auto">
        <div class="p-6">
          <div class="text-center mb-6">
            <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-2">¡Empecemos!</h3>
            <p class="text-sm text-gray-600 dark:text-gray-400">Sigue estos pasos para configurar tu restaurante</p>
          </div>
          <ol class="space-y-4">
            <% has_categories = current_restaurant.categories.any? %>
            <li class="flex items-center gap-3 p-3 <%= has_categories ? 'bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-700' : 'bg-gray-50 dark:bg-gray-800' %> rounded-lg">
              <span class="flex-shrink-0 w-6 h-6 <%= has_categories ? 'bg-green-500' : 'bg-blue-500' %> text-white rounded-full flex items-center justify-center text-sm font-medium">
                <% if has_categories %>
                  ✓
                <% else %>
                  1
                <% end %>
              </span>
              <span class="text-gray-900 dark:text-white font-medium">
                <% if has_categories %>
                  ✅ Categoría creada
                <% else %>
                  Crea una <%= link_to "categoría", admin_categories_path, class: "text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 underline font-medium" %>
                <% end %>
              </span>
            </li>
            <li class="flex items-center gap-3 p-3 bg-gray-50 dark:bg-gray-800 rounded-lg">
              <span class="flex-shrink-0 w-6 h-6 bg-blue-500 text-white rounded-full flex items-center justify-center text-sm font-medium">2</span>
              <span class="text-gray-900 dark:text-white font-medium">Añade un <%= link_to "plato", new_dish_path, class: "text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 underline font-medium" %></span>
            </li>
            <li class="flex items-center gap-3 p-3 bg-gray-50 dark:bg-gray-800 rounded-lg">
              <span class="flex-shrink-0 w-6 h-6 bg-blue-500 text-white rounded-full flex items-center justify-center text-sm font-medium">3</span>
              <span class="text-gray-900 dark:text-white font-medium">Descarga tu código QR</span>
            </li>
            <li class="flex items-center gap-3 p-3 bg-gray-50 dark:bg-gray-800 rounded-lg">
              <span class="flex-shrink-0 w-6 h-6 bg-blue-500 text-white rounded-full flex items-center justify-center text-sm font-medium">4</span>
              <span class="text-gray-900 dark:text-white font-medium">Ofrece el QR a tus clientes</span>
            </li>
            <li class="flex items-center gap-3 p-3 bg-gradient-to-r from-green-50 to-blue-50 dark:from-green-900/20 dark:to-blue-900/20 rounded-lg border border-green-200 dark:border-green-700">
              <span class="flex-shrink-0 w-6 h-6 bg-green-500 text-white rounded-full flex items-center justify-center text-sm font-medium">5</span>
              <span class="text-gray-900 dark:text-white font-medium">Disfruta de Cartly ☺️</span>
            </li>
          </ol>
        </div>
      </div>
    </div>
  <% end %>
<% end %>
